#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int maracuya,lagartija,amazonas;
	printf("introduzca un numero:");
	scanf("%d", &maracuya);
	printf("¿desea colocar otro numero ((1)s/(2)n)?:");
	scanf("%d", &lagartija);
		
	
	 
	while(lagartija != 2); 

		printf("introduzca un numero:");
	    scanf("%d", &amazonas);
	
		amazonas=maracuya+maracuya ;
		
		printf("¿desea colocar otro numero ((1)s/(2)n)?:");
	    scanf("%d", &lagartija);
	
	  
	
	
	
}
